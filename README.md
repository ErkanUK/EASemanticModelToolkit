# EA Semantic Model Toolkit

A unified Sparx Enterprise Architect 17 add-in for importing JSON/YAML semantic models and exporting EA packages to LinkML, JSON Schema, Markdown, diagrams, and OWL.

The importer and exporter histories are retained under `components/importer` and `components/exporter`.
